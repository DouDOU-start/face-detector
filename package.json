{
  "name": "face-detector-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build outputs for FaceDetector (minified + obfuscated)",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "terser": "^5.44.0"
  }
}
